{
  "name": "opencomm-booking",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Who gets a slot in this round. Owns CommissionWindow, CommissionRequest and the Pricelist module.",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "migrate": "node --env-file-if-exists=.env src/migrate.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
