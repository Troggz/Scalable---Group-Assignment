{
  "name": "opencomm-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Making commissioned artwork and releasing it after payment.",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "migrate": "node --env-file-if-exists=.env src/migrate.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
