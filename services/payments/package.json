{
  "name": "opencomm-payments",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Collecting money exactly once. Owns Invoice.",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "migrate": "node --env-file-if-exists=.env src/migrate.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
